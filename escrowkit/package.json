{
  "name": "escrowkit",
  "version": "0.1.0",
  "description": "SDK and CLI for deploying milestone-based escrow contracts on Stellar Soroban",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "escrowkit": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "cli": "ts-node src/cli/index.ts"
  },
  "keywords": ["stellar", "soroban", "escrow", "smart-contracts", "web3", "dao", "milestone"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@stellar/stellar-sdk": "^12.0.0",
    "commander": "^12.0.0",
    "chalk": "^5.3.0",
    "ora": "^7.0.1",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "ts-node": "^10.9.0"
  }
}
